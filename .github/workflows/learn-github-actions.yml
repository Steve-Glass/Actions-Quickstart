name: learn-github-actions-testing
on: [push]
jobs:
  build:
    runs-on: macos-12-xl
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: swift build
      - name: Run tests
        run: swift test
